# Investigation Limitations

This is a source-derived controlled adversary-emulation case, not a real victim incident.

Principal limitations:

- no Initial Access delivery telemetry,
- no proof of credential extraction outcome,
- no exfiltration receiver/object evidence,
- no real business-impact or recovery execution,
- no direct event-level Zeek corroboration because host and network-sensor capture windows do not overlap,
- Day 2 is out of scope.

Scenario 01–20 are not incident evidence.

See `evidence/processed/investigation-limitations.csv`.
