# Scope Assessment

The final scope is deliberately narrower than the full dataset.

**Affected/compromised:** SCRANTON and NASHUA.

**Affected/misused identity:** `DMEVALS\pbeesly`.

**Incident-related lab network observables:** `192.168.0.4` and `192.168.0.5`.

**Observed but not established compromised:** NEWYORK and UTICA.

**Data scope:** `working.zip` is referenced by collection/staging, but its contents and transfer outcome are unknown.

Cloud identities and business services cannot be assessed because those context/telemetry domains are not present.

See `evidence/processed/scope-assessment.csv`.
