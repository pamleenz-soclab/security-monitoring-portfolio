# Evidence Inventory

## Raw evidence — local only

Generated by `scripts/acquisition/scenario17_generate_synthetic_dataset.py`:

| File | Purpose |
|---|---|
| `entra-signins.jsonl` | Interactive, non-interactive, service-principal, and managed-identity sign-ins |
| `identity-protection-risk-detections.jsonl` | Platform-generated risk signals |
| `entra-directory-audit.jsonl` | Security-information changes and containment actions |
| `m365-unified-audit.jsonl` | Inbox-rule creation and file downloads |
| `business-context.json` | User baseline, known devices, approved networks, and user verification |
| `ground-truth.json` | Synthetic author ground truth; validation only |
| `schema-basis.json` | Official schema references and normalization notes |
| `DATASET-LICENSE.txt` | CC0 notice |
| `acquisition-manifest.json` | Generated-file sizes and SHA-256 hashes |

Raw files are never committed.

## Working evidence — local only

- normalized SQLite database;
- schema and time profiles;
- identity, device, application, IP, MFA, CA, risk, and identifier inventories;
- password-spray, MFA-fatigue, legacy-authentication, travel, and token-refresh candidates;
- precise timeline and stable-ID correlation;
- console logs and temporary manifests.

Working files are never committed.

## Processed evidence — publishable

All processed files use aliases for identities and stable identifiers. Synthetic RFC 5737 IP addresses are retained because network relationships are part of the investigation.

| File | Purpose |
|---|---|
| `cloud-identity-event-timeline.csv` | Sanitised incident chronology |
| `identity-and-signin-type-summary.csv` | Identity and sign-in-type inventory |
| `application-and-resource-summary.csv` | Application, resource, protocol, and result summary |
| `source-ip-and-location-analysis.csv` | Network, ASN, location, and approved-network analysis |
| `device-context-analysis.csv` | Managed, compliant, trusted, unknown, and unavailable device context |
| `authentication-result-analysis.csv` | Success, failure, error-code, and requirement interpretation |
| `mfa-method-and-result-analysis.csv` | MFA methods, outcomes, and evidence boundaries |
| `conditional-access-analysis.csv` | Enforced, not-applied, and report-only policy results |
| `risk-signal-assessment.csv` | Platform-risk events kept separate from facts |
| `correlation-and-session-analysis.csv` | Sign-in, request, correlation, session, and token scopes |
| `legacy-authentication-analysis.csv` | Blocked IMAP/ROPC attempt |
| `mfa-fatigue-assessment.csv` | Repeated MFA failures followed by successful approval |
| `password-spray-assessment.csv` | Distributed spray candidates |
| `follow-on-activity-analysis.csv` | Identity changes, mailbox rule, file access, and response actions |
| `account-compromise-assessment.csv` | Fact, risk, verification, ground truth, gap, and conclusion use |
| `detection-gap-analysis.csv` | Missing or intentionally excluded evidence |
| `sanitised-evidence-excerpts.tsv` | Compact interview and report excerpts |
| `source-sha256-records.tsv` | Integrity record for local raw sources |

## Integrity

The precise verifier confirmed:

- 10 of 10 local source hashes matched;
- 162 sign-in IDs were unique;
- no orphan authentication steps;
- no orphan Conditional Access policy records;
- first-pass and SQLite record counts aligned.

A first-pass manifest warning was recorded because the console log continued changing after that manifest was generated. The final package manifest is generated only after all publishable files are closed.
