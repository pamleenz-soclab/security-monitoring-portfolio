# Investigation Queries

These are read-only investigation examples. They must be adapted to the tenant's available tables, retention, field mappings, and normalisation. No query connects to a tenant by itself.
